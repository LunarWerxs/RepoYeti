{
  "name": "repoyeti-web",
  "private": true,
  "version": "0.21.3",
  "license": "MIT",
  "type": "module",
  "//test": "Web tests run under VITEST via `bun run test` — NOT `bun test`. Bun's native runner has no Vue SFC compiler, so `.vue` imports resolve to a string path and @vue/test-utils crashes (WeakMap key error). Same reason CI uses `bun run test`.",
  "scripts": {
    "dev": "vite",
    "//build": "Builds land in dist-next, then swap-dist renames it over dist. The daemon serves dist live — building in place left it empty/partial for the whole build (the Monaco chunk-404 bug).",
    "build": "node scripts/i18n-check.mjs && vue-tsc -b && vite build --outDir dist-next && node scripts/swap-dist.mjs",
    "build:fast": "vite build --outDir dist-next && node scripts/swap-dist.mjs",
    "preview": "vite preview",
    "//typecheck": "The web half of the typecheck. The root `typecheck` is tsc over the daemon only, so a template referencing a store field that does not exist type-checks clean there — that is exactly how the changed-file 'View all' control shipped reading an undefined property. Incremental, so repeat runs are near-instant.",
    "typecheck": "vue-tsc -b",
    "i18n:check": "node scripts/i18n-check.mjs",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:e2e": "playwright test"
  },
  "dependencies": {
    "@formkit/auto-animate": "^0.8.2",
    "@formkit/drag-and-drop": "^0.6.1",
    "@lucide/vue": "^1.22.0",
    "@vueuse/core": "^14.3.0",
    "class-variance-authority": "^0.7.1",
    "clsx": "^2.1.1",
    "dompurify": "3.4.13",
    "marked": "14.0.0",
    "monaco-editor": "^0.56.0",
    "pinia": "^2.3.0",
    "qrcode": "^1.5.4",
    "reka-ui": "^2.10.1",
    "tailwind-merge": "^3.6.0",
    "vue": "^3.5.39",
    "vue-i18n": "^11",
    "vue-sonner": "^2.0.9"
  },
  "devDependencies": {
    "@iconify-json/vscode-icons": "^1.2.62",
    "@playwright/test": "^1.61.1",
    "@tailwindcss/vite": "^4.3.1",
    "@types/qrcode": "^1.5.6",
    "@vitejs/plugin-vue": "^5.2.1",
    "@vue/compiler-sfc": "^3.5.39",
    "@vue/test-utils": "^2.4.11",
    "happy-dom": "^20.10.6",
    "tailwindcss": "^4.3.2",
    "tw-animate-css": "^1.4.0",
    "typescript": "^6.0.3",
    "unplugin-icons": "^23.0.1",
    "vite": "^8.1.0",
    "vite-plugin-pwa": "^1.3.0",
    "vitest": "^4.1.9",
    "vue-tsc": "^3.3.5"
  },
  "//overrides": "Do NOT pin brace-expansion to one version. minimatch 5/9 want ^2 (`module.exports = expand`); minimatch 10 — what workbox-build's glob uses — wants ^5 (named `expand` export). A flat 2.1.2 pin made the PWA build throw '(0 , brace_expansion_1.expand) is not a function' inside workbox's globbing, which is only a *warning*: the build stayed green while the precache silently shrank to the 6 public/ files, so the app shell was never cached offline. Bun does not support nested overrides (it warns and ignores them), so each requester must resolve on its own; every line is past the CVE-2025-5889 ReDoS fix (1.1.12 / 2.0.2 / 3.0.1 / 4.0.1), so natural resolution stays patched.",
  "//dompurify": "Pinned exact, and repeated in overrides so monaco-editor's transitive copy resolves to the same one. Keep both lines equal and at or above 3.4.13: 3.4.12 and below leave a detached subtree executable after an IN_PLACE hook removal (GHSA-55q2-fjhq-7xh7), and this app sanitises README and commit-message markdown from arbitrary cloned repos, so that path takes untrusted input by design.",
  "overrides": {
    "dompurify": "3.4.13",
    "fast-uri": "3.1.5"
  },
  "patchedDependencies": {
    "@formkit/auto-animate@0.8.4": "patches/@formkit%2Fauto-animate@0.8.4.patch",
    "@formkit/drag-and-drop@0.6.1": "patches/@formkit%2Fdrag-and-drop@0.6.1.patch",
    "local-pkg@1.2.1": "patches/local-pkg@1.2.1.patch",
    "vue-sonner@2.0.9": "patches/vue-sonner@2.0.9.patch"
  }
}
