[test]
# THE TEST TIMEOUT LIVES IN THE TEST FILES, and `timeout` does NOT belong under this table.
# Every file whose tests spawn calls `useSuiteTimeout()` (tests/helpers/timeouts.ts) at its top,
# which is scoped to that file and holds for the whole run however many files are in it. That is
# what makes a bare `bun test` and `bun run test` the SAME suite - a timeout on the command line
# only applies when someone remembers the command line, and forgetting it once already produced
# a retracted "Bun 1.4 broke this repo" claim built entirely on the wrong invocation.
#
# The two tidier-looking places were tried and MEASURED on bun 1.4.0, and both are false greens:
#   - `timeout = 20000` under this [test] table is IGNORED outright. A 6s test still dies at 5s.
#   - `setDefaultTimeout()` from the preload below applies only when ONE file runs; add a second
#     file to the invocation and every test is back on 5s.
# scripts/checks/spawn-test-without-timeout.mjs enforces the per-file call and no longer stands
# itself down for either of them.
preload = ["./tests/setup.ts"]
# web/ is Vitest's, not bun:test's. Bun's runner has no Vue SFC compiler, so a `.vue` import
# resolves to a *string* path; @vue/test-utils then calls WeakMap.set(<string>) and throws
# "WeakMap keys must be objects". The failure looks like a library bug but is just the wrong
# runner. `bun run test` (in web/) drives Vitest and is what CI uses. Ignoring web/ here keeps a
# bare `bun test` at the root honest; without it, it sweeps web/test/** and reports ~42 phantom
# failures against a suite that is actually green.
pathIgnorePatterns = ["web/**"]
# NOTE: bun's built-in `coverageThreshold` is PER-FILE, which is too brittle for this repo (the
# integration entrypoints daemon.ts/index.ts are legitimately low-coverage by design). Instead the
# coverage gate lives in scripts/check-coverage.ts, which runs the suite with coverage and enforces
# an OVERALL line-coverage floor (MIN_TEXT_LINE_COVERAGE / MIN_LCOV_LINE_COVERAGE). Run it via
# `bun run check:coverage`; CI runs that script in place of a bare `bun test` (see .github/workflows/ci.yml).
